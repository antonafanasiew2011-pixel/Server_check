# ⚙️ Добавлен фильтр по системам

## 📋 Обзор

В раздел "Фильтры и поиск" добавлен новый фильтр по системам, который позволяет пользователям фильтровать серверы по типу системы.

## ✨ Новые возможности

### 1. **⚙️ Фильтр по системам**

#### **Доступные типы систем:**
- **🌐 Web Server** - веб-серверы
- **🗄️ Database** - базы данных
- **⚙️ Application** - приложения
- **📊 Monitoring** - системы мониторинга
- **💾 Backup** - системы резервного копирования
- **⚖️ Load Balancer** - балансировщики нагрузки
- **🚀 Cache** - кэш-серверы
- **📧 Mail Server** - почтовые серверы
- **📁 File Server** - файловые серверы
- **🌍 DNS Server** - DNS серверы
- **🔒 Proxy Server** - прокси-серверы
- **❓ Other** - прочие системы

### 2. **📊 Сортировка по системам**

#### **Новая опция сортировки:**
- **⚙️ Система** - сортировка по названию системы (алфавитный порядок)

## 🔧 Технические детали

### **Frontend изменения:**

#### **HTML (templates/servers.html):**
```html
<div class="form-group">
  <label class="form-label">Система</label>
  <select name="system" class="form-control">
    <option value="">Все системы</option>
    <option value="Web Server">🌐 Web Server</option>
    <option value="Database">🗄️ Database</option>
    <!-- ... другие опции ... -->
  </select>
</div>
```

#### **Сортировка:**
```html
<option value="system">⚙️ Система</option>
```

### **Backend изменения:**

#### **Роутер (app/routers.py):**
```python
async def list_servers(
    # ... другие параметры ...
    system: str | None = None,  # system name filter
):
```

#### **Фильтрация:**
```python
if system:
    enriched = [e for e in enriched if e[0].system_name == system]
```

#### **Сортировка:**
```python
elif sort == "system":
    key_func = lambda e: (e[0].system_name or "").lower()
```

#### **Параметры шаблона:**
```python
"params": {
    # ... другие параметры ...
    "system": system or ""
}
```

## 🎯 Использование

### **Фильтрация по системам:**
1. Откройте страницу серверов
2. В разделе "Фильтры и поиск" найдите поле "Система"
3. Выберите нужный тип системы из выпадающего списка
4. Нажмите "Применить фильтры"

### **Сортировка по системам:**
1. В разделе "Сортировка и отображение" выберите "⚙️ Система"
2. Серверы будут отсортированы по названию системы в алфавитном порядке

### **Комбинирование фильтров:**
- Фильтр по системам можно комбинировать с другими фильтрами
- Например: показать только веб-серверы в продакшене
- Или: показать только базы данных с высокой загрузкой CPU

## 📊 Результат

### **Улучшенная фильтрация:**
✅ **Фильтр по системам** с 12 предустановленными типами  
✅ **Сортировка по системам** в алфавитном порядке  
✅ **Интеграция с существующими фильтрами**  
✅ **Сохранение состояния** фильтра в URL  
✅ **Визуальные иконки** для каждого типа системы  

### **Пользовательский опыт:**
- **Быстрая фильтрация** по типу системы
- **Интуитивные иконки** для визуального различения
- **Совместимость** с существующими фильтрами
- **Сохранение предпочтений** при навигации

## 🎉 Заключение

Фильтр по системам значительно улучшает возможности поиска и фильтрации серверов, позволяя пользователям быстро находить серверы определенного типа и эффективно управлять инфраструктурой.

**Фильтр готов к использованию!** 🚀
